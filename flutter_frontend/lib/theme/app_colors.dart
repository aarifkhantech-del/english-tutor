import 'package:flutter/material.dart';

class AppColors {
  // Dark palette
  static const Color background = Color(0xFF0F172A); // Deep slate
  static const Color surface = Color(0xFF1E293B);    // Slate card surface
  static const Color cardBg = Color(0xFF334155);     // Soft dark card

  // Accents
  static const Color primary = Color(0xFF6366F1);    // Indigo primary
  static const Color primaryLight = Color(0xFF818CF8);
  static const Color secondary = Color(0xFF0EA5E9);  // Cyan secondary
  static const Color accent = Color(0xFF10B981);     // Emerald green

  // Recording State
  static const Color recordingRed = Color(0xFFEF4444);
  static const Color recordingGlow = Color(0x66EF4444);

  // Status & Text
  static const Color textPrimary = Color(0xFFF8FAFC);
  static const Color textSecondary = Color(0xFF94A3B8);
  static const Color textMuted = Color(0xFF64748B);

  // Correction Highlights
  static const Color correctionBorder = Color(0xFF6366F1);
  static const Color explanationBg = Color(0x1F0EA5E9);
  static const Color practiceBg = Color(0x1F10B981);
  static const Color encouragementBg = Color(0x1F8B5CF6);
}
